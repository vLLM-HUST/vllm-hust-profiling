#!/usr/bin/env bash

# By default, use and record the current HEAD of the checked-out repositories.
# Uncomment or export these variables before running when a pinned commit is required:
# export VLLM_COMMIT=<vllm-commit>
# export VLLM_ASCEND_COMMIT=<vllm-ascend-commit>
export PROFILER_TIMELIMIT=600
