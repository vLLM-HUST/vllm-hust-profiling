#!/usr/bin/env bash

# Dataset/model paths are configured in ../config/paths.env.
# Keep this file as the workload contract shared by the team.
export RANDOM_INPUT_LEN=1024
export RANDOM_OUTPUT_LEN=256
export REQUEST_RATE=1
export PROFILER_TIMELIMIT=600
